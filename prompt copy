Hi. I want you to create a new script based on the existing xtm-ar script. Below I outline the new features. What I don't mention specifically means that thing should work the same as in the xtm-ar script. For example:
- Mackie mode
- Working in a group track
- Track pinning
- Led handling
- Logging
- Code patterns: setting up the trackbank, getting references to tracks/remote pages, remote page tagging, observers, getting devices, launching/stopping midi clips etc.
- For reference on these pattarns you can also check the script xtm-pro.

Ok so here's how the script should work.

- The name of the new script is xtm-kit-single. It will be very similar to xtm-ar but much simpler.
- Encoders map to the page tagged 'perform' on the Group Track.
- Upper buttons map to the page tagged 'mutes' on the Group Track.
- Bottom buttons launch scenes on the group track. We bring over the hold-based clip stopping function.
- When Layer A is active, Encoders map to 'a' on the Group Track.
- When Layer B is active, Encoders map to 'b' on the Group Track.
- Now the encoder pushes: They will control the instrument selecctor. This can be a bit tricky as we need to navigate to the Instrument Selector that is placed on the group track itself. There is an example of this in xtm-pro. I think we are using primary device to get the selector. This might need a workaround as there is a midi device in front of the instrument selector. If you cannot do this I can find a workaround and remove that device but it would be nice.


Development goes in 3 phases:
1) Initial skeleton setup, I load in bitwig and test the initial load (via debug messages).
2) Implement the rest of the functionality step by step. You build, I test, you fix.
    - encoders
    - buttons
    - layer a
    - layer b
    - etc. each is a step
4) Wait for future requests

Make sure you only bring over code that is actually required; do not leave dead code in the script.